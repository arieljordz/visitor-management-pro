import React, { useState, useEffect } from 'react';
import AuthForm from "../components/auth/AuthForm";

const Auth: React.FC = () => {
        return (
          <div>
           <AuthForm/>
          </div>
        );

    };

export default Auth;